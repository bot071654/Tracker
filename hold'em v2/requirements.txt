# Poker Hand Tracker - runtime dependencies (Python 3.12+)
opencv-python>=4.9
numpy>=1.26
mss>=9.0
psycopg[binary]>=3.1
openpyxl>=3.1
python-dotenv>=1.0

# Action Controller (automation/mouse_controller.py) - clicks the LOCAL TEST
# poker table only, and only when config/mouse_controller.json enables it.
pyautogui>=0.9.54

# Only used by tools/generate_templates.py to render the starter templates.
pillow>=10.0

# Tests
pytest>=8.0
