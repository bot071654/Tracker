# Poker Hand Tracker - runtime dependencies (Python 3.12+)
opencv-python>=4.9
numpy>=1.26
mss>=9.0
psycopg[binary]>=3.1
openpyxl>=3.1
python-dotenv>=1.0

# Spoken announcements (voice/). Windows SAPI5 through pyttsx3: offline, no
# account and no API key. Optional - with voice_enabled false in
# config/config.json it is never imported, and the tracker runs without it.
pyttsx3>=2.90

# Only used by tools/generate_templates.py to render the starter templates.
pillow>=10.0

# Tests
pytest>=8.0
